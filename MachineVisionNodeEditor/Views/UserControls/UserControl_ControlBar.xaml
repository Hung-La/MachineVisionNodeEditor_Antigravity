<UserControl x:Class="MachineVisionNodeEditor.Views.UserControls.UserControl_ControlBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             xmlns:i="clr-namespace:Microsoft.Xaml.Behaviors;assembly=Microsoft.Xaml.Behaviors"
             xmlns:local="clr-namespace:MachineVisionNodeEditor.Views.UserControls"
             xmlns:UserControlViewModel="clr-namespace:MachineVisionNodeEditor.ViewModels.UserControlViewModels"
             mc:Ignorable="d" 
             Name="ControlBar"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=UserControlViewModel:UserControl_ControlBarViewModel}">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="MouseLeftButtonDown">
            <i:InvokeCommandAction Command="{Binding DragMove}" CommandParameter="{Binding ElementName=ControlBar}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <materialDesign:ColorZone
           Mode="PrimaryLight">
            <DockPanel>

                <DockPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="Black" CornerRadius="5" BorderThickness="2">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"></ContentPresenter>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="RenderTransform">
                                                <Setter.Value>
                                                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"></ScaleTransform>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                            <Setter Property="Effect">
                                                <Setter.Value>
                                                    <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.5"></DropShadowEffect>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                    
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DockPanel.Resources>
                
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Margin" Value="1 2 1 2"></Setter>
                            <Setter Property="Width" Value="35"></Setter>
                        </Style>
                    </StackPanel.Resources>
                    <Button
                       Command="{Binding MinimizeWindowCommand}"
                       CommandParameter="{Binding ElementName=ControlBar}"
                       Content="{materialDesign:PackIcon Kind=WindowMinimize}"
                       Background="LightBlue"
                       ToolBar.OverflowMode="AsNeeded"
                       ToolTip="Minimize" />
                    <Button
                       Command="{Binding MaximizeWindowCommand}"
                       CommandParameter="{Binding ElementName=ControlBar}"
                       Content="{materialDesign:PackIcon Kind=WindowMaximize}"
                       Background="LightGreen"
                       ToolBar.OverflowMode="AsNeeded"
                       ToolTip="Maximize" />
                    <Separator />
                    <Button
                       Command="{Binding CloseWindowCommand}"
                       CommandParameter="{Binding ElementName=ControlBar}"
                       Content="{materialDesign:PackIcon Kind=WindowClose}"
                       ToolBar.OverflowMode="AsNeeded"
                       Background="OrangeRed"
                       ToolTip="Close" />

                </StackPanel>
                <StackPanel
                   Orientation="Horizontal">
                    <!--<ToggleButton
                       Style="{StaticResource MaterialDesignHamburgerToggleButton}" />-->
                    <Image Height="26" VerticalAlignment="Stretch" Source="/Images/Icon-removebg-preview.png" Margin="2 2 2 2" />
                    <TextBlock
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       Text="{Binding ElementName=ControlBar, Path=Tag}" />
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>
    </Grid>
</UserControl>
